import 'package:flutter/material.dart';

class LinkedProductTab extends StatelessWidget {
  const LinkedProductTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('General'),
      ),
    );
  }
}
